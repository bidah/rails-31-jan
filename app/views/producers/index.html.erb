<div class="col-md-12">
  <h1>Producers#index</h1>
  <p>Here I will show all producers</p>

  <ol>
  <% @producers.each do |producer| %>
    <li>
      <%= link_to "#{producer.name}: #{producer.description}", producer_path(producer) %>
    </li>
  <% end %>
  </ol>
</div>